// import PRODUCTS from '../../data/dummy-data';
// import { createSlice } from '@reduxjs/toolkit';
// import Product from '../../models/product'
// export const initialState = {
//     wallet: []
//   };
  
//   const walletSlice = createSlice({
//     name: 'wallet',
//     initialState,
//     reducers: {
//       addWallet(state,action) {
//         const wal = {}
//         wal.id = action.payload.id;
//         wal.balance= action.payload.balance;
//         wal.name = action.payload.idCategory;
//         wal.description = action.payload.description;
//         wal.startDate = action.payload.startDate;
//         wal.idTrans = action.payload.idTrans;
//         state.wallet.wal(trans)
//       }
//     }
//   })
  
//   export const { addWallet } = walletSlice.actions
//   export default walletSlice.reducer